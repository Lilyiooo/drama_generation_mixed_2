"""剧本格式要求prompt"""

SCRIPT_FORMAT_PROMPT = """
请以中文纯文本形式输出最终剧本（禁止使用Markdown格式），严格按照以下格式规范撰写：

#### 1. 场头（必须严格遵守）
每场剧本的**第一行**必须是场头，场头**单独占一行**，格式如下：
```
{{EpisodeIndex}}-1. [地点] [时间] 人物：[角色A]、[角色B]、[角色C]
```
场头各要素说明：
- **场号**：`{{EpisodeIndex}}-1`，集号与场次号之间用`-`连接，场号后紧跟`.`和一个空格
- **地点**：简洁的场景地名，**每场只能有一个地点**，不可罗列多个地点。如：藏经阁
- **时间**：紧跟地点之后，用空格分隔，取值为：日 / 夜 / 晨 / 昏
- **人物**：时间之后空两格写`人物：`，列出本场**所有出场角色**，角色之间用`、`分隔，按主要→次要顺序排列。**注意：人物只写人名，不要在人名后加括号标注身份/职业等信息；不要将"VO系统""旁白"等非角色名写入人物列表**
- **场头之后空一行再开始正文**，场头行本身不包含任何正文内容

注意：场头是剧本工业的标准格式，**绝对不能省略、合并或拆分**。即使只有一个角色也必须写全场头。

#### 2. 旁白与动作描述格式（严格控制比例）
- **硬性比例要求**：每场剧本中，角色对话（含OS/VO）的字数必须占该场总字数的 **70% 以上**，旁白与动作描述（△）合计**不超过 30%**。
- 使用 `△` 符号标记旁白和动作描述，`△` 符号与后面的描述之间空一个空格。
- **△ 仅允许用于以下场景**：
    - 场景开头的极简环境交代（如"△ 夜色深沉，书房内烛火摇曳。"）
    - 角色出入场的简短动作（如"△ 甲推门而入。"）
    - 无法通过对话传达的关键视觉信息（如"△ 桌上放着一封血书。"）
- **△ 禁止用于**：
    - 连续描写角色的动作序列（应通过对话中的括号注释体现，如 `甲（攥紧拳头）：...`）
    - 描写角色的表情变化或情绪反应
    - 对已通过对话表达的信息进行重复描述
- **频率限制**：禁止连续出现 2 句及以上的 `△` 描述，每两句 `△` 之间必须至少间隔 2 句角色对话。
- 单句动作描述字数限制在15字以内。

**❌ 错误示范（旁白过多，违反7:3比例）**：
```
△ 甲缓缓走进房间，环顾四周。
△ 房间里光线昏暗，窗帘紧闭。
△ 甲注意到桌上有一封信，走过去拿起来。
△ 甲的手微微颤抖，拆开信封。
甲：这是……
```

**✅ 正确示范（对话为主，符合7:3比例）**：
```
△ 甲推门而入。
甲：怎么把窗帘都拉上了？（拉开窗帘）
乙：你先别管窗帘，看看桌上那封信。
甲（拿起信封，手微颤）：这是……谁写的？
乙：你自己拆开看看就知道了。
甲：这笔迹……是父亲的？！
```

#### 3. 对话格式
- 格式：`角色名：台词内容`
- **对话内容必须占每场总字数的 70% 以上**，这是硬性要求。剧情推进、信息传递、情感表达都应优先通过角色对话来实现。

#### 4. OS与VO格式
- OS（画外音/内心独白）：在角色名后标注 `OS`，格式：`角色名OS：台词内容`
- VO（旁白配音）：在角色名后标注 `VO`，格式：`角色名VO：台词内容`

#### 5. 制作效果描述与动作括号注释
- 关于人物情绪、拍摄手法、剪辑手法、服化妆造等制作效果的描述，可用（）加以注明，不做硬性要求。
- 示例：`甲（愤怒）：你到底在说什么！`
- **禁止括号动作描述单独占一行**：`（xxx）` 格式的动作/情绪描述**必须**紧跟在角色名之后、冒号之前，作为台词行的一部分，绝不允许单独成行。如果动作无法附着在台词上，则必须改用 `△` 旁白格式。
- **❌ 错误示范**（括号动作单独占一行）：
```
云曦（抓起缴费单，疯了一样冲向ICU病房区）：阿爸，等着我，我来了！
（她冲到护士站，把单据"啪"地拍在桌上。）
云曦：缴了！我缴清了！快！快给我爸用药！
```
- **✅ 正确示范**（动作融入台词行或使用△格式）：
```
云曦（抓起缴费单，疯了一样冲向ICU病房区）：阿爸，等着我，我来了！
△ 她冲到护士站，把单据"啪"地拍在桌上。
云曦：缴了！我缴清了！快！快给我爸用药！
```

#### 6. 闪回设置
- 闪回内容需要与正文相隔一行，用【闪回】标记开始，用【闪回结束】标记结束。
- 如果是闪回到前面已有的内容，需写明闪回到第几集第几场。
- 闪回结束后，如果回到原场景继续，需注明"接X-X"（集号-场次号）。
- 格式示例：

△乙从来到丙身边。
乙：丙，我们看到金光就冲过来了，来的还及时吧？
（相隔一行）
【闪回】西郊厂房附近 夜 外
出场人物：丙、乙
△ 丙将一叠符递给一旁的乙。
丙：我已通知族长他们，乙你拿着这些等他们，等会你们就贴上这符在厂房上空盘旋，记住以金光为讯号！
【闪回结束】接3-1
（相隔一行）
△甲和他的乙以及各自身的女友丙和丁，四人在游艇上嬉闹。
△乙把火锅里一块牛肉喂进甲嘴里，甲十分享受地咀嚼着。
甲OS：今天风平浪静天气好，我和女朋友乙还有我的死党丙和他女朋友丁，在游艇上吃着火锅，唱着歌。


### 7. 场景一致性原则（必须严格遵守）
- **一场戏只能发生在一个场景中**，不允许在同一场次内通过"镜头切到""切回"等方式跳转到其他场景。
- 如果剧情需要切换场景，必须开启新的一场（新场头），不可在当前场次内描写其他地点的画面。
- 错误示例：`1-1. 深山 日 人物：甲` 正文中出现 `△镜头切回至古朴的房间内`——这是错误的，深山和房间内是两个不同场景，必须分为两场。

### 8. 场次分隔格式
- 每场之间用`\n\n---\n\n`分割
- 单场剧本内所有内容（△描述、对话、OS等）紧挨着写，无需空行。除非需要闪回时，按闪回格式要求相隔一行。

### 9. 正文结束
一集的正文内容结束后需空一行且左顶格标注（第{{EpisodeIndex}}集完）。

### 10. 完整格式示例
{{EpisodeIndex}}-1. 房间内 日 人物：甲、乙

△ 甲猛地从床上惊醒，发现自己上身赤裸地躺在床上，然后好奇的打量着四周。（出姓名条：甲）
...

---

{{EpisodeIndex}}-2. 市场水路 夜 人物：丙、校尉、商贩们

△ 俯瞰镜头：四条船同时驶入市场。
校尉：**钦差办案，闲杂人等速速闪开！**
...

（第{{EpisodeIndex}}集完）
"""
