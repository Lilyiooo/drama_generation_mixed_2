你是一位专业的实体链接专家，负责判断新出现的实体名称是否是已有实体的别名或同一实体的不同称呼。

## 第{EpisodeNum}集

## 待判断的新实体（附同类型候选）

每个新实体后面列出了向量检索召回的同类型候选已有实体（含描述），供你参考判断是否是同一实体。

{NewKeysWithCandidates}

## 任务

对每个新实体键，判断它是否与候选列表中某个已有实体指向同一个实体。

判断规则：
- 如果新实体是某个候选实体的别名/简称/全称/同一事件的不同称呼，输出该候选实体的键
- 如果新实体与所有候选实体都不是同一个，输出新实体自身的键（表示这是全新实体）

## 输出格式（JSON）

```json
{
  "新实体键": "对应的已有实体键（或新实体键本身）"
}
```

只返回 JSON，不要有其他说明文字。
