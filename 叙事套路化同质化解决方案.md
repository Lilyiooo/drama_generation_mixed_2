## 短剧叙事套路化 / 同质化问题解决方案

> 本文档总结当前（v8）在「局部树搜索」事件线生成链路中，针对"叙事套路化 / 同质化"问题所采用的方法。
> 对应实现：`tools/stage_eventline_local_tree_v8.py` 及 `tools/stage_eventline_pipeline_v8.py`。

---

### 问题定义

短剧事件线扩展（把粗粒度锚点扩写成细粒度事件链）过程中，模型容易陷入**"换对象重复同一套路"**，导致不同锚点段、不同 stage 的剧情高度同质。典型表现：

- 反复"公开闹场施压"（哭嚎骂街、摆摊唱戏、制造舆论）
- 反复"扮猪吃老虎武力碾压"（隐藏实力、一招制服）
- 反复"挑拨离间 / 将计就计反杀"

同质化一旦累积，观众会在连续事件里反复看到"换汤不换药"的推进方式，削弱新鲜感和爽点。

---

### 总体思路

把"去重"从模糊的**软提示**，升级为**显式记忆 + 两级评分闸门 + 权重约束**：

- 用「叙事套路 bank（trope_bank）」**显式记录**已经用过的因果套路
- 在**两级评分闸门**的 `novelty` 维度里**对照 bank 打分**，复用套路被压低
- 通过**权重**与**措辞**，让"换皮复用"在打分阶段就被压制，而不是生成后再去事后补救

---

### 核心机制

#### 树结构（局部树搜索）

事件线不是一次性写完整段，而是用「局部树搜索」逐层扩展：从空的根节点出发，每层给当前活跃的叶子节点生成若干"下一事件"候选，打分后只保留最优的继续往下长，直到抵达目标锚点或触达深度上限。一个锚点段会被拆成多轮这样的局部树搜索。

```
ROOT（depth=0）
├─ 候选 A1 ─ 候选 A1.1 ─ 候选 A1.1.1 ...   （到达锚点 → completed_paths）
├─ 候选 A2 ─ 候选 A2.1 ...                 （保留 top-k，继续扩展）
├─ 候选 A3                                 （novelty 低，被剪枝）
└─ 候选 A4 ─ ...                           （保留 top-k，继续扩展）
```

搜索流程：

- 对每个活跃叶子节点，并行生成 `branch_factor`（默认 4）个"下一事件"候选
- 第一道闸门给候选打分，`choose_top_candidates_for_expansion` 只保留 `keep_top_k`（默认 2）个继续扩展，其余剪枝
- 到达目标锚点的候选进入 `completed_paths`；未到达的进入下一层继续扩展
- 达到 `local_max_depth`（默认 4）仍未到锚点的，归入 `incomplete_paths`
- `completed_paths` + `incomplete_paths` 共同构成 `terminal_chains`，交给第二道闸门评分，选一条提交

关键参数：

| 参数 | 默认 | 含义 |
|---|---|---|
| branch_factor | 4 | 每个节点生成的候选数 |
| keep_top_k | 2 | 每层保留继续扩展的候选数 |
| local_max_depth | 4 | 单轮局部链最大深度 |
| max_events_to_anchor | 12 | 单锚点段累计最大事件数 |
| max_local_chains_per_anchor | 4 | 单锚点段最多轮数 |

若一轮局部链未到锚点，会以"已提交事件"为固定前缀继续开启下一轮，直到到达锚点或轮数 / 事件数触顶。

#### 叙事套路 bank（trope_bank）

从已经提交的事件链里，用 LLM 提炼**「起因 -> 手段 -> 结果 -> 反应」四元组**的微观因果链套路：

| 槽位 | 含义 | 示例 |
|---|---|---|
| 起因 cause | 触发点 | 对方有求 |
| 手段 method | 推进方式 | 制造诱饵 |
| 结果 outcome | 直接结果 | 目标上钩 |
| 反应 response | 后续反应 | 提出条件 |

提取约束（提取 prompt 要求）：

- **抽象化**：每个槽位用 2-8 字词组，**不出现具体人名 / 地点 / 道具**
- 事件链中可能存在多条套路链，需**逐一罗列**

提取与传递时机：

- 启动时：若有前序 stage，先对**历史事件**建库
- 每提交一个锚点段后：提取该段套路并**追加**
- 跨段、跨 stage **累积传递**（`--initial-trope-bank` 复用前序 stage 已建好的 bank，避免重复建库）

#### 两级评分闸门

事件线生成是一个"局部树搜索"：每层生成多个候选 → 打分 → 剪枝保留 top-k → 继续扩展，最终产出多条终端链 → 评分选一条提交。

| 闸门 | 作用 | novelty 是否对照 bank |
|---|---|---|
| 第一道闸门：候选步评分 | 树内扩展时，对"同一父节点下的下一事件候选"打分并 keep_top_k 剪枝 | 是（v8 加入） |
| 第二道闸门：局部链评分 | 对最终产生的多条终端链打分，选一条提交 | 是 |

两个闸门的 `novelty` 都显式对照 bank 去重，是整套方案的核心。

---

### 关键设计

#### 评分权重

- 第一道闸门（v8 调整）：

  `expansion_score = 0.30·logic + 0.20·drive + 0.30·novelty + 0.20·appeal`

- 第二道闸门：

  `score = 0.30·coherence + 0.30·novelty + 0.30·target_fit + 0.10·continuation_value + (到达锚点 ? 0.35 : 0)`

#### novelty 打分措辞

两个闸门的 `novelty` 定义都显式要求"换皮复用必须低分"，并追加"链内同质化也要扣分"：

> 评价它相对"已用叙事套路 bank"中记录的因果链（起因->手段->结果->反应），是否引入了新的因果推进模式，而非复用已固定的完整前缀事件中的叙事套路（包含前序 stage 与本 stage 已完成部分）；若本候选/本条链的核心套路与 bank 中某条高度雷同或相似度较高（换皮复用），novelty 必须给低分；若本候选所属的事件链内部存在叙事同质化、套路化的问题，也应适度下调 novelty 得分。

#### few-shot 引导

用正反例示范"换对象重复旧套路 → novelty 低分"：

- 反例：`女主继续在尚书府门口哭嚎骂街，逼对方拿钱息事宁人` → `novelty = 3`（仍是"公开闹场施压"，换对象重复旧套路）
- 正例：`团队先摸清尚书最爱名画、最怕赝品丑闻，再借一场假拍卖逼他露怯` → `novelty = 8`（从硬闹切换到设局智取，跳脱前序套路）

#### 生成侧约束

候选生成 prompt 里同步加入两条硬约束：

- `尽量不要复用已固定的完整前缀事件中的叙事套路（包含前序 stage 与本 stage 已完成部分）`
- `不要为了显得新就跳步，不要提前把后续锚点的核心悬念直接消耗掉`

即在**生成端**也压一遍，而不是只靠评分端事后打分。

#### 容错重试

第一道闸门评分若返回非 JSON 对象导致解析失败，自动**重试最多 3 次**（递增 sleep），3 次仍失败才报错，避免单次 LLM 输出抖动直接中断整条流水线。

---

### bank 里实际存了什么（举例）

`trope_bank` 在结果 JSON 里就是一条条四元组，每条固定 4 个字段：

```json
{
  "cause": "对方有求",
  "method": "制造诱饵",
  "outcome": "目标上钩",
  "response": "提出条件"
}
```

实际跑出来的 bank 大致长这样（摘录自 v6 / v7 结果）：

| 起因 | 手段 | 结果 | 反应 |
|---|---|---|---|
| 应对内斗 | 缔结假婚 | 建立合作 | 共同入局 |
| 恶人欺压 | 示弱反击 | 实力碾压 | 掌控局势 |
| 陷入僵局 | 公开揭短 | 舆论施压 | 对方失控 |
| 对方有求 | 制造诱饵 | 目标上钩 | 提出条件 |
| 对方使诈 | 将计就计 | 诡计失败 | 加倍奉还 |
| 目标有弱点 | 设局利诱 | 赢回钱财 | 额外获利 |
| 双方有矛盾 | 挑拨离间 | 两败俱伤 | 坐收渔利 |
| 对方重名声 | 制造舆论 | 颜面尽失 | 设局反击 |

评分时，这份 bank 会被格式化成一行行 `起因:X -> 手段:Y -> 结果:Z -> 反应:W` 的可读文本，注入 `novelty` 评分 prompt，供模型判断"本候选/本条链的核心套路是否与 bank 中某条高度雷同"。

可以看到，bank 里每条都是一套**完整的因果推进闭环**，而不是孤立的事件；同一条链可能对应一个事件，也可能对应两个甚至多个事件共同构成的套路。

---

### 已知局限与后续方向

- **无绝对拒绝阈值**：第二道闸门始终"从本轮链里选最高分"提交，没有"novelty 低于某阈值就整轮重采样"的硬过滤，若整轮候选都偏套路就只能"矮子里拔将军"
- **bank 段级更新滞后**：bank 在"整个锚点段提交后"才追加，同一段内的多轮局部链之间看不到彼此刚提交的套路
- **抽象粒度 vs 换皮复用**：语义四元组容易把"换皮复用"洗成"不同套路"（同结构不同包装时，method 槽位措辞不同就不算重复），对"深层结构相同、包装不同"的重复基本失效
- **锚点层同构**：当输入锚点本身要求"逐一 XX"时（如"逐一揭晓身份"），这是输入端写死的同构，局部链层无法规避，需要回到锚点生成层解决

---

### 版本演进速览

| 版本 | 关键改动 |
|---|---|
| v4 | 跨段去重 prompt（约束：不复用固定前缀叙事套路） |
| v5 | 剩余事件预算与 `max_events_to_anchor` 关联 |
| v6 | 引入 trope_bank（起因->手段->结果->反应），局部链 novelty 对照 bank |
| v7 | bank 跨 run 复用（`--initial-trope-bank`）、novelty 内部重复惩罚 |
| v8 | 第一道闸门 novelty 对照 bank + 权重调整为 logic/novelty 各 30% + 评分重试 |
